import { ApproveDirective } from './approve.directive';

describe('ApproveDirective', () => {
  it('should create an instance', () => {
    const directive = new ApproveDirective();
    expect(directive).toBeTruthy();
  });
});
