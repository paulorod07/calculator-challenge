import { ReproveDirective } from './reprove.directive';

describe('ReproveDirective', () => {
  it('should create an instance', () => {
    const directive = new ReproveDirective();
    expect(directive).toBeTruthy();
  });
});
